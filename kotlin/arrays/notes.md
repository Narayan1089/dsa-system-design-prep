# Arrays (Kotlin)

- Input type on LeetCode: `IntArray` (not `Array<Int>` for primitives)
- Indices: `for (i in nums.indices)` or `nums.forEachIndexed { i, v -> }`
- Return pair: `intArrayOf(i, j)`

See `TwoSum.kt` and `../practice/HashMapTwoSumWalkthrough.kt`.
