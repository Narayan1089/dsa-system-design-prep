# Heap / Priority Queue

NeetCode 150 #59–63. See `phase1/neetcode150-map.md`.

```kotlin
import java.util.PriorityQueue

val minHeap = PriorityQueue<Int>()           // smallest first
val maxHeap = PriorityQueue<Int>(reverseOrder())
```
