# Hashing (Kotlin)

- `hashSetOf<Int>()` — unique elements, `n in set`
- `hashMapOf<K, V>()` — key → value; `map[key]` returns nullable
- `mutableMapOf`, `mutableSetOf` when you need to mutate

See `ContainsDuplicate.kt` and `arrays/TwoSum.kt`.
