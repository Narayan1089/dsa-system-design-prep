# Queue (Kotlin)

Theory: see `dsa/queue/notes.md`

```kotlin
val q = ArrayDeque<Int>()
q.addLast(10)   // offer
q.removeFirst() // poll
q.first()       // peek
```

BFS and level-order traversal use a queue.
